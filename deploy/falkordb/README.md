# FalkorDB for HarborRAG

FalkorDB can be used as a graph-store option for graph expansion, cross-source linking, and memory relationships.

The initial online milestone does not require graph storage. Add FalkorDB only after the vector-only golden path is stable.
