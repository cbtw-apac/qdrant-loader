# Qdrant for HarborRAG

Qdrant is the recommended first vector store for the online golden path.

Default local endpoint:

```text
http://localhost:6333
```

The collection name should be configured by HarborRAG runtime configuration, not hard-coded in application logic.
